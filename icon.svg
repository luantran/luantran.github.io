<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
    <!-- Background -->
    <rect width="100" height="100" fill="#1e293b" rx="12"/>

    <!-- Left bracket -->
    <text x="-11" y="68" font-family="'Courier New', monospace" font-size="80" fill="#06b6d4" font-weight="bold">{</text>

    <!-- Letter L -->
    <text x="50" y="72" font-family="'Courier New', monospace" font-size="80" fill="#ffffff" font-weight="bold" text-anchor="middle">L</text>

    <!-- Right bracket -->
    <text x="63" y="68" font-family="'Courier New', monospace" font-size="80" fill="#06b6d4" font-weight="bold">}</text>
</svg>